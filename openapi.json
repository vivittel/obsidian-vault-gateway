{
  "components": {
    "schemas": {
      "ErrorCode": {
        "description": "The error code vocabulary (IMPLEMENTATION_PLAN section 13).\n\nTwo codes need a note:\n\n* ``RATE_LIMITED`` is part of the published contract but is never raised in\n  Phase 1 — no rate limiting is implemented yet.\n* ``VALIDATION_ERROR`` is an addition to section 13's list (which the plan\n  calls \"主なエラーコード\", i.e. non-exhaustive). Request validation failures\n  that are not about a path or a title — a non-integer ``limit``, an unknown\n  body field — need a code of their own rather than being forced into\n  ``INVALID_PATH``.",
        "enum": [
          "UNAUTHORIZED",
          "INVALID_PATH",
          "PATH_OUTSIDE_VAULT",
          "NOTE_NOT_FOUND",
          "INVALID_FILE_TYPE",
          "FILE_TOO_LARGE",
          "INVALID_TITLE",
          "NOTE_ALREADY_EXISTS",
          "VALIDATION_ERROR",
          "RATE_LIMITED",
          "INVALID_CURSOR",
          "NOTE_MODIFIED",
          "INBOX_LOCK_TIMEOUT",
          "INTERNAL_ERROR"
        ],
        "title": "ErrorCode",
        "type": "string"
      },
      "ErrorDetail": {
        "properties": {
          "code": {
            "$ref": "#/components/schemas/ErrorCode"
          },
          "message": {
            "title": "Message",
            "type": "string"
          }
        },
        "required": [
          "code",
          "message"
        ],
        "title": "ErrorDetail",
        "type": "object"
      },
      "ErrorResponse": {
        "description": "The single error shape used by every failing response (section 13).",
        "properties": {
          "error": {
            "$ref": "#/components/schemas/ErrorDetail"
          }
        },
        "required": [
          "error"
        ],
        "title": "ErrorResponse",
        "type": "object"
      },
      "HealthResponse": {
        "properties": {
          "inbox_writable": {
            "description": "The inbox mount is writable.",
            "title": "Inbox Writable",
            "type": "boolean"
          },
          "status": {
            "description": "'degraded' when a mount is missing or has the wrong permissions.",
            "enum": [
              "ok",
              "degraded"
            ],
            "title": "Status",
            "type": "string"
          },
          "vault_readable": {
            "description": "The read-only vault mount is readable.",
            "title": "Vault Readable",
            "type": "boolean"
          }
        },
        "required": [
          "status",
          "vault_readable",
          "inbox_writable"
        ],
        "title": "HealthResponse",
        "type": "object"
      }
    }
  },
  "info": {
    "description": "Health-only REST API (docs/adr/0010-*.md). MCP (mounted at /mcp) is the sole functional interface — full-vault search, note reads, vault tree/summary browsing, inbox duplicate-candidate detection, and note creation/append restricted to 00_Inbox/ChatGPT are all MCP tools, not REST routes. This REST API exists only for `docker healthcheck`/Caddy/curl-based diagnostics against GET /api/v1/health. Every failing response uses the single `{\"error\": {\"code\": ..., \"message\": ...}}` envelope described on each operation below — never FastAPI's default `{\"detail\": ...}` shape.",
    "title": "Obsidian Vault Gateway",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/v1/health": {
      "get": {
        "operationId": "getHealth",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "`error.code` is one of: INTERNAL_ERROR."
          },
          "default": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Every failing response — including any status code not listed individually above — uses this same envelope; only `error.code` and `error.message` vary."
          }
        },
        "summary": "Report whether the vault mounts are usable",
        "tags": [
          "health"
        ]
      }
    }
  }
}
