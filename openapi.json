{
  "components": {
    "schemas": {
      "AppendedNoteResponse": {
        "properties": {
          "appended_bytes": {
            "description": "Bytes added to the file, including any inserted separator/terminator.",
            "title": "Appended Bytes",
            "type": "integer"
          },
          "id": {
            "description": "Vault-relative path of the note.",
            "title": "Id",
            "type": "string"
          },
          "modified_at": {
            "description": "Modification time in the configured timezone.",
            "format": "date-time",
            "title": "Modified At",
            "type": "string"
          },
          "path": {
            "description": "Vault-relative path of the note.",
            "title": "Path",
            "type": "string"
          }
        },
        "required": [
          "id",
          "path",
          "modified_at",
          "appended_bytes"
        ],
        "title": "AppendedNoteResponse",
        "type": "object"
      },
      "ChatExport": {
        "additionalProperties": false,
        "description": "Structured summary of a conversation, ready to be formatted into a note.\n\nOne flat model rather than a discriminated union, because `mode` must\ndefault to \"summary\" when the client omits it and a pydantic discriminated\nunion requires the tag to be present. Fields that do not belong to the\nselected `mode` are rejected by app/services/chat_export.py — inside the\ntool body, so the rejection travels through _McpCall's GatewayError\nconversion instead of the SDK's raw ToolError path.\n\n`related_notes` carries only the client's candidate paths — it is not what\ngets rendered. app/services/related_notes.py re-verifies every path\nagainst the Vault (this model cannot: it has no filesystem access), and\napp/services/chat_export.render_chat_export takes the verified survivors\nthrough a separate `verified_related_notes` argument, never this field\ndirectly. See docs/adr/0006-verified-related-note-wikilinks.md.",
        "properties": {
          "context": {
            "description": "technical mode only. The problem or constraints the design starts from.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Context",
            "type": "array"
          },
          "conversation_type": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional. A short free-form label for the kind of conversation, e.g. 'design', 'debugging', 'review'. Omit it rather than guessing.",
            "title": "Conversation Type"
          },
          "decisions": {
            "description": "Only conclusions that were actually adopted or choices that were explicitly agreed. Not options still under discussion, and not things left to do — those belong in unresolved_issues and next_actions, never here. Leave empty when nothing was decided.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Decisions",
            "type": "array"
          },
          "definitions": {
            "description": "reference mode only. Term and meaning pairs.",
            "items": {
              "$ref": "#/components/schemas/TermDefinition"
            },
            "maxItems": 50,
            "title": "Definitions",
            "type": "array"
          },
          "design": {
            "description": "technical mode only. The design or approach itself, one point per item.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Design",
            "type": "array"
          },
          "environment": {
            "description": "issue mode only. Versions, hosts, configuration relevant to the problem.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Environment",
            "type": "array"
          },
          "examples": {
            "description": "reference mode only. Concrete examples, one per item.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Examples",
            "type": "array"
          },
          "facts": {
            "description": "reference mode only. Standalone facts worth looking up later.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Facts",
            "type": "array"
          },
          "implementation_notes": {
            "description": "technical mode only. Details that matter when implementing: file names, function names, gotchas.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Implementation Notes",
            "type": "array"
          },
          "investigation": {
            "description": "issue mode only. What was checked and what it showed, in order.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Investigation",
            "type": "array"
          },
          "key_points": {
            "description": "summary mode only. The points worth remembering, one per item.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Key Points",
            "type": "array"
          },
          "mode": {
            "default": "summary",
            "description": "Which export shape to render. Use 'summary' for a plain 'summarise this and save it' request and whenever no other mode clearly fits. 'technical': a design or implementation discussion. 'history': how something changed over time. 'full': a long conversation covering several distinct topics. 'procedure': repeatable steps someone will follow later. 'issue': a problem, its investigation, and its cause. 'reference': terms, facts, and examples to look up later.",
            "enum": [
              "summary",
              "technical",
              "history",
              "full",
              "procedure",
              "issue",
              "reference"
            ],
            "title": "Mode",
            "type": "string"
          },
          "next_actions": {
            "description": "Concrete things someone will do next, one per item. Keep this separate from unresolved_issues — a task is not an open question, and the two must never be merged. Leave empty when there is nothing to do next.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Next Actions",
            "type": "array"
          },
          "overview": {
            "description": "summary mode only. What the conversation was about, in a few points.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Overview",
            "type": "array"
          },
          "prerequisites": {
            "description": "procedure mode only. What must already be true before starting.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Prerequisites",
            "type": "array"
          },
          "project": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional. The project or repository this belongs to, when the conversation names one. Omit it rather than guessing.",
            "title": "Project"
          },
          "related_notes": {
            "description": "Vault-relative .md paths of existing notes this conversation relates to — call search_notes first and pass paths exactly as it returned them; never invent or guess a path. Usually 3-5 items, at most 10. The Gateway re-verifies every path when writing the note: a path it cannot verify is left out rather than blocking the export, so the rendered '## 関連ノート' section — not this input — is the record of what actually got linked.",
            "items": {
              "type": "string"
            },
            "maxItems": 10,
            "title": "Related Notes",
            "type": "array"
          },
          "rollback": {
            "description": "procedure mode only. How to undo it if it goes wrong.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Rollback",
            "type": "array"
          },
          "root_cause": {
            "description": "issue mode only. The established cause. Leave empty when the cause was not established — do not put a guess here.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Root Cause",
            "type": "array"
          },
          "sources": {
            "description": "Where the information came from: a URL, a document name, a command that was run. One per item.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Sources",
            "type": "array"
          },
          "steps": {
            "description": "procedure mode only, and required for it. One action per item, in the order they must be performed. Rendered as a numbered list; do not put your own numbering in the text.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 50,
            "title": "Steps",
            "type": "array"
          },
          "symptom": {
            "description": "issue mode only, and required for it. What was observed going wrong — the behaviour, not the cause.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Symptom",
            "type": "array"
          },
          "tags": {
            "description": "Open-ended Obsidian tags you choose for this note. No fixed vocabulary. Write them without a leading '#'; spaces are replaced with '-'. title, created, updated, source and export_mode are generated by the Gateway and cannot be set here.",
            "items": {
              "maxLength": 200,
              "type": "string"
            },
            "maxItems": 20,
            "title": "Tags",
            "type": "array"
          },
          "timeline": {
            "description": "history mode only, and required for it. Events in the order they happened, oldest first.",
            "items": {
              "$ref": "#/components/schemas/TimelineEntry"
            },
            "maxItems": 50,
            "title": "Timeline",
            "type": "array"
          },
          "tldr": {
            "description": "Required. Normally 2-5 short sentences covering the subject, the conclusion, and the current state. One sentence per item; they are joined into a single paragraph directly under the title.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 8,
            "minItems": 1,
            "title": "Tldr",
            "type": "array"
          },
          "topics": {
            "description": "full mode only, and required for it. One entry per distinct topic the conversation covered, in the order they came up. Each becomes a '###' subsection.",
            "items": {
              "$ref": "#/components/schemas/TopicSection"
            },
            "maxItems": 20,
            "title": "Topics",
            "type": "array"
          },
          "turning_points": {
            "description": "history mode only. The moments where the direction actually changed.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Turning Points",
            "type": "array"
          },
          "unresolved_issues": {
            "description": "Questions still open: things not yet decided or not yet known. Keep this separate from next_actions — an open question is not a task, and the two must never be merged. Leave empty when nothing is open.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Unresolved Issues",
            "type": "array"
          },
          "verification": {
            "description": "technical and procedure modes only. How to confirm it works: tests, commands, expected output.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Verification",
            "type": "array"
          },
          "workaround": {
            "description": "issue mode only. What makes it usable without a full fix.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Workaround",
            "type": "array"
          }
        },
        "required": [
          "tldr"
        ],
        "title": "ChatExport",
        "type": "object"
      },
      "CreatedNoteResponse": {
        "properties": {
          "id": {
            "description": "Vault-relative path of the created note.",
            "title": "Id",
            "type": "string"
          },
          "modified_at": {
            "description": "Creation time in the configured timezone.",
            "format": "date-time",
            "title": "Modified At",
            "type": "string"
          },
          "path": {
            "description": "Vault-relative path of the created note.",
            "title": "Path",
            "type": "string"
          },
          "related_notes_linked": {
            "description": "Number of export.related_notes candidates that were verified and rendered as wikilinks. Always 0 for the raw content/frontmatter path.",
            "minimum": 0.0,
            "title": "Related Notes Linked",
            "type": "integer"
          },
          "related_notes_skipped": {
            "description": "Number of export.related_notes candidates omitted because they could not be verified — invalid, missing, or duplicate. Always 0 for the raw content/frontmatter path. Submitting more than the documented maximum is a separate, harder failure: the whole request is rejected before this count is ever produced.",
            "minimum": 0.0,
            "title": "Related Notes Skipped",
            "type": "integer"
          },
          "title": {
            "description": "Sanitised file-name stem of the created note, without .md. May differ from the H1 and frontmatter title of a structured export.",
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "id",
          "path",
          "title",
          "modified_at",
          "related_notes_linked",
          "related_notes_skipped"
        ],
        "title": "CreatedNoteResponse",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "HealthResponse": {
        "properties": {
          "inbox_writable": {
            "description": "The inbox mount is writable.",
            "title": "Inbox Writable",
            "type": "boolean"
          },
          "status": {
            "description": "'degraded' when a mount is missing or has the wrong permissions.",
            "enum": [
              "ok",
              "degraded"
            ],
            "title": "Status",
            "type": "string"
          },
          "vault_readable": {
            "description": "The read-only vault mount is readable.",
            "title": "Vault Readable",
            "type": "boolean"
          }
        },
        "required": [
          "status",
          "vault_readable",
          "inbox_writable"
        ],
        "title": "HealthResponse",
        "type": "object"
      },
      "InboxNoteAppendRequest": {
        "additionalProperties": false,
        "properties": {
          "content": {
            "description": "Markdown to append. The existing note's line ending is preserved.",
            "maxLength": 2000000,
            "minLength": 1,
            "title": "Content",
            "type": "string"
          },
          "path": {
            "description": "Vault-relative path of an existing .md note directly inside the inbox.",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Path",
            "type": "string"
          }
        },
        "required": [
          "path",
          "content"
        ],
        "title": "InboxNoteAppendRequest",
        "type": "object"
      },
      "InboxNoteCreateRequest": {
        "additionalProperties": false,
        "description": "Exactly one of `content` or `export` is required; sending both, or\nsending neither, is rejected. `export` and `frontmatter` are also\nmutually exclusive — `export`'s frontmatter (title/created/updated/\nsource/export_mode/tags) is formatter-owned and cannot be supplied as\narbitrary free-form frontmatter alongside it. `content`/`frontmatter`\nremain the raw-Markdown path kept for existing callers; `export` is the\nstructured chat-export path (issue #12 / docs/adr/0005-*.md).",
        "properties": {
          "content": {
            "anyOf": [
              {
                "maxLength": 2000000,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Markdown body. Written as-is with LF line endings. Exactly one of `content` or `export` is required.",
            "title": "Content"
          },
          "export": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ChatExport"
              },
              {
                "type": "null"
              }
            ],
            "description": "Structured chat-export input (issue #12). The Gateway renders this into deterministic Markdown and frontmatter; the client never supplies raw Markdown or frontmatter alongside it. Exactly one of `content` or `export` is required."
          },
          "frontmatter": {
            "anyOf": [
              {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "integer"
                    },
                    {
                      "type": "number"
                    },
                    {
                      "type": "boolean"
                    },
                    {
                      "items": {
                        "anyOf": [
                          {
                            "type": "string"
                          },
                          {
                            "type": "integer"
                          },
                          {
                            "type": "number"
                          },
                          {
                            "type": "boolean"
                          },
                          {
                            "type": "null"
                          }
                        ]
                      },
                      "type": "array"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional YAML frontmatter for the `content` path. Scalars and flat lists of scalars only. Rejected together with `export`, whose frontmatter is formatter-owned.",
            "title": "Frontmatter"
          },
          "title": {
            "description": "Human-readable title. The file name is derived from it by the API; callers cannot choose a path.",
            "maxLength": 300,
            "minLength": 1,
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "title"
        ],
        "title": "InboxNoteCreateRequest",
        "type": "object"
      },
      "NoteResponse": {
        "properties": {
          "content": {
            "description": "Markdown body with the frontmatter block removed.",
            "title": "Content",
            "type": "string"
          },
          "frontmatter": {
            "additionalProperties": true,
            "description": "Parsed YAML frontmatter. Empty when absent or unparseable.",
            "title": "Frontmatter",
            "type": "object"
          },
          "id": {
            "description": "Vault-relative path of the note.",
            "title": "Id",
            "type": "string"
          },
          "modified_at": {
            "description": "File mtime in the configured timezone.",
            "format": "date-time",
            "title": "Modified At",
            "type": "string"
          },
          "path": {
            "description": "Vault-relative path of the note.",
            "title": "Path",
            "type": "string"
          },
          "title": {
            "description": "Frontmatter `title`, else the file name without .md.",
            "title": "Title",
            "type": "string"
          },
          "truncated": {
            "description": "True when the note exceeded MAX_NOTE_SIZE_BYTES and content was cut.",
            "title": "Truncated",
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "path",
          "title",
          "frontmatter",
          "content",
          "modified_at",
          "truncated"
        ],
        "title": "NoteResponse",
        "type": "object"
      },
      "SearchResponse": {
        "properties": {
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Opaque pagination token. Pass it back as `cursor` with the same query/folder/tags to fetch the next page. Null when there is no further page.",
            "title": "Next Cursor"
          },
          "results": {
            "items": {
              "$ref": "#/components/schemas/SearchResultItem"
            },
            "title": "Results",
            "type": "array"
          }
        },
        "required": [
          "results"
        ],
        "title": "SearchResponse",
        "type": "object"
      },
      "SearchResultItem": {
        "properties": {
          "excerpt": {
            "description": "Short plain-text snippet around the match.",
            "title": "Excerpt",
            "type": "string"
          },
          "id": {
            "description": "Vault-relative path; pass it to readNote as `path`.",
            "title": "Id",
            "type": "string"
          },
          "modified_at": {
            "description": "File mtime in the configured timezone.",
            "format": "date-time",
            "title": "Modified At",
            "type": "string"
          },
          "path": {
            "description": "Vault-relative path of the note.",
            "title": "Path",
            "type": "string"
          },
          "tags": {
            "description": "Frontmatter tags, in file order.",
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "title": {
            "description": "Frontmatter `title`, else the file name without .md.",
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "id",
          "path",
          "title",
          "excerpt",
          "tags",
          "modified_at"
        ],
        "title": "SearchResultItem",
        "type": "object"
      },
      "TermDefinition": {
        "additionalProperties": false,
        "description": "One term/description pair, for `reference` mode.",
        "properties": {
          "description": {
            "description": "What it means, in one sentence.",
            "maxLength": 1000,
            "title": "Description",
            "type": "string"
          },
          "term": {
            "description": "The term being defined.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Term",
            "type": "string"
          }
        },
        "required": [
          "term",
          "description"
        ],
        "title": "TermDefinition",
        "type": "object"
      },
      "TimelineEntry": {
        "additionalProperties": false,
        "description": "One dated or ordered event, for `history` mode.",
        "properties": {
          "event": {
            "description": "What happened, in one sentence.",
            "maxLength": 1000,
            "title": "Event",
            "type": "string"
          },
          "when": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When it happened, exactly as the conversation stated it — a date, a phase name, or a relative marker such as 'after the review'. Omit it when the conversation did not say; never guess a date.",
            "title": "When"
          }
        },
        "required": [
          "event"
        ],
        "title": "TimelineEntry",
        "type": "object"
      },
      "TopicSection": {
        "additionalProperties": false,
        "description": "One '###' subsection of a `full` export.",
        "properties": {
          "heading": {
            "description": "Short label for this topic, rendered as a '###' subheading.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Heading",
            "type": "string"
          },
          "points": {
            "description": "What was covered under this topic, one point per item.",
            "items": {
              "maxLength": 1000,
              "type": "string"
            },
            "maxItems": 30,
            "title": "Points",
            "type": "array"
          }
        },
        "required": [
          "heading"
        ],
        "title": "TopicSection",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "ctx": {
            "title": "Context",
            "type": "object"
          },
          "input": {
            "title": "Input"
          },
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      },
      "VaultNameCount": {
        "properties": {
          "name": {
            "title": "Name",
            "type": "string"
          },
          "note_count": {
            "title": "Note Count",
            "type": "integer"
          }
        },
        "required": [
          "name",
          "note_count"
        ],
        "title": "VaultNameCount",
        "type": "object"
      },
      "VaultSummaryResponse": {
        "properties": {
          "folder_count": {
            "description": "Number of distinct folders that directly contain a note.",
            "title": "Folder Count",
            "type": "integer"
          },
          "last_modified_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Newest note mtime in the configured timezone. Null if empty.",
            "title": "Last Modified At"
          },
          "note_count": {
            "description": "Total number of Markdown notes in the vault.",
            "title": "Note Count",
            "type": "integer"
          },
          "skipped_count": {
            "description": "Notes that could not be read while aggregating and were excluded.",
            "title": "Skipped Count",
            "type": "integer"
          },
          "tags": {
            "description": "Most common frontmatter tags, folded for case/width, up to `top_tags_limit`.",
            "items": {
              "$ref": "#/components/schemas/VaultNameCount"
            },
            "title": "Tags",
            "type": "array"
          },
          "top_level_folders": {
            "description": "Note counts per top-level folder. A note directly at the vault root is not counted here.",
            "items": {
              "$ref": "#/components/schemas/VaultNameCount"
            },
            "title": "Top Level Folders",
            "type": "array"
          },
          "total_bytes": {
            "description": "Combined file size of every Markdown note, in bytes.",
            "title": "Total Bytes",
            "type": "integer"
          }
        },
        "required": [
          "note_count",
          "total_bytes",
          "folder_count",
          "top_level_folders",
          "tags",
          "skipped_count"
        ],
        "title": "VaultSummaryResponse",
        "type": "object"
      },
      "VaultTreeEntry": {
        "properties": {
          "modified_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "File mtime in the configured timezone. Null for folders.",
            "title": "Modified At"
          },
          "name": {
            "description": "File or folder name, without any path component.",
            "title": "Name",
            "type": "string"
          },
          "path": {
            "description": "Vault-relative path.",
            "title": "Path",
            "type": "string"
          },
          "type": {
            "enum": [
              "folder",
              "note"
            ],
            "title": "Type",
            "type": "string"
          }
        },
        "required": [
          "type",
          "name",
          "path"
        ],
        "title": "VaultTreeEntry",
        "type": "object"
      },
      "VaultTreeResponse": {
        "properties": {
          "entries": {
            "items": {
              "$ref": "#/components/schemas/VaultTreeEntry"
            },
            "title": "Entries",
            "type": "array"
          },
          "folder": {
            "description": "Vault-relative folder that was listed. Empty for the root.",
            "title": "Folder",
            "type": "string"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Opaque pagination token. Pass it back as `cursor` with the same `folder` to fetch the next page. Null when there is no further page.",
            "title": "Next Cursor"
          }
        },
        "required": [
          "folder",
          "entries"
        ],
        "title": "VaultTreeResponse",
        "type": "object"
      }
    },
    "securitySchemes": {
      "bearerAuth": {
        "description": "Send `Authorization: Bearer <API_TOKEN>`.",
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "description": "Read-mostly gateway over an Obsidian vault: full-vault search, note reads, and note creation restricted to 00_Inbox/ChatGPT. MCP (mounted at /mcp) is the primary interface; this REST API is kept for health checks, curl-based diagnostics, and regression tests.",
    "title": "Obsidian Vault Gateway",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/v1/health": {
      "get": {
        "operationId": "getHealth",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Report whether the vault mounts are usable",
        "tags": [
          "health"
        ]
      }
    },
    "/api/v1/inbox/notes": {
      "post": {
        "operationId": "createInboxNote",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InboxNoteCreateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreatedNoteResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "summary": "Create a Markdown note in 00_Inbox/ChatGPT",
        "tags": [
          "inbox"
        ]
      }
    },
    "/api/v1/inbox/notes/append": {
      "post": {
        "operationId": "appendInboxNote",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InboxNoteAppendRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppendedNoteResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "summary": "Append Markdown to an existing note in 00_Inbox/ChatGPT",
        "tags": [
          "inbox"
        ]
      }
    },
    "/api/v1/notes": {
      "get": {
        "operationId": "readNote",
        "parameters": [
          {
            "description": "Vault-relative path, e.g. 'Knowledge/PC/GPU/RTX 5070.md'.",
            "in": "query",
            "name": "path",
            "required": true,
            "schema": {
              "description": "Vault-relative path, e.g. 'Knowledge/PC/GPU/RTX 5070.md'.",
              "title": "Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NoteResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "summary": "Read a note by its vault-relative path",
        "tags": [
          "notes"
        ]
      }
    },
    "/api/v1/search": {
      "get": {
        "operationId": "searchNotes",
        "parameters": [
          {
            "description": "Free-text search term.",
            "in": "query",
            "name": "q",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Free-text search term.",
              "title": "Q"
            }
          },
          {
            "description": "Vault-relative folder to restrict the search to.",
            "in": "query",
            "name": "folder",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Vault-relative folder to restrict the search to.",
              "title": "Folder"
            }
          },
          {
            "description": "Comma-separated frontmatter tags; all must match.",
            "in": "query",
            "name": "tags",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Comma-separated frontmatter tags; all must match.",
              "title": "Tags"
            }
          },
          {
            "description": "Maximum number of results.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 20,
              "description": "Maximum number of results.",
              "maximum": 200,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Opaque pagination token from a previous response.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque pagination token from a previous response.",
              "title": "Cursor"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SearchResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "summary": "Search the vault by file name, title, tags, headings and body",
        "tags": [
          "search"
        ]
      }
    },
    "/api/v1/vault/summary": {
      "get": {
        "operationId": "getVaultSummary",
        "parameters": [
          {
            "description": "Maximum number of tags to return.",
            "in": "query",
            "name": "top_tags_limit",
            "required": false,
            "schema": {
              "default": 20,
              "description": "Maximum number of tags to return.",
              "maximum": 200,
              "minimum": 1,
              "title": "Top Tags Limit",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VaultSummaryResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "summary": "Summarise vault-wide note counts, sizes, and tags",
        "tags": [
          "vault"
        ]
      }
    },
    "/api/v1/vault/tree": {
      "get": {
        "operationId": "getVaultTree",
        "parameters": [
          {
            "description": "Vault-relative folder to list. Omit for the vault root.",
            "in": "query",
            "name": "folder",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Vault-relative folder to list. Omit for the vault root.",
              "title": "Folder"
            }
          },
          {
            "description": "Maximum number of entries.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 100,
              "description": "Maximum number of entries.",
              "maximum": 500,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Opaque pagination token from a previous response.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque pagination token from a previous response.",
              "title": "Cursor"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VaultTreeResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ],
        "summary": "List the direct children of a vault folder",
        "tags": [
          "vault"
        ]
      }
    }
  }
}
