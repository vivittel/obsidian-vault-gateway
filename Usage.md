# Obsidian Vault Gateway の使い方

この文書は、`vivittel/obsidian-vault-gateway` を日常的に使う人と、保存後の整理を担当する Codex などのエージェント向けの実用ガイドです。

## まず覚えること

Obsidian Vault Gateway の役割は、次の二つです。

1. Vault 全体を検索・参照する
2. ChatGPT から作ったノートを `00_Inbox/ChatGPT` に安全に新規保存する、または明示的な指示に従って追記する

Vault 内のノートをどこへ移すか、何と統合するか、分割するか、削除するかは Gateway の仕事ではありません。これらは人間または Codex などの別のツールで行います。

したがって、通常の利用者は export mode や内部ツール名を意識する必要はありません。残したい情報量や形式を、普段の言葉で指定すれば十分です。

> この会話を Obsidian に保存して

> この会話を、会話の流れや検討内容が分かるようにフルで Obsidian に保存して

> この会話をもとに、前提条件・手順・検証方法・ロールバックを含む手順書を作って Obsidian に保存して

それぞれ、通常の要約保存、会話内容を広く残す保存、再実行可能な手順書としての保存に対応します。クライアントが意図を読み取り、適切な export mode を選びます。

Gateway に渡された会話の内容を、クライアントが構造化して保存します。Gateway 自身が会話を要約したり、保存先を意味的に分類したりすることはありません。

## 前提条件と利用環境

### Docker Compose で起動する

Obsidian Vault Gateway は Docker コンテナとして動作し、リポジトリの `compose.yaml` を使って Docker Compose で構成・起動します。実行環境には Docker Engine と Docker Compose が必要です。

サーバーには、Gateway が読み取る Obsidian Vault と、書き込み先である `00_Inbox/ChatGPT` を配置し、`compose.yaml` からコンテナへマウントする必要があります。

Gateway 自体に Vault の同期機能はありません。PC、スマートフォン、サーバーなど複数の端末で同じ Vault を利用する場合は、Obsidian の同期用プラグインや別の同期手段を用意してください。開発者の環境では、`Self-hosted LiveSync` と `livesync-cli` を組み合わせ、サーバー上の Vault と各端末を同期しています。これは利用例であり、Gateway の必須構成ではありません。

### MCP サーバーを公開するかどうか

MCP サーバーをインターネットへ公開するかどうかは、利用形態に応じて選択できます。

| 構成 | 主な利用方法 | 検証状況 |
|---|---|---|
| 非公開 | LAN、VPN、Tailscale などから到達できる ChatGPT デスクトップ、Codex CLI、Codex IDE extension などのローカルクライアントから接続する | 開発者が利用・検証している構成 |
| 公開 | インターネットから到達可能な remote MCP として構成し、ChatGPT Web からプラグイン経由で利用する | 開発者は未検証 |

非公開構成で ChatGPT から利用する場合は、MCP サーバーへ到達できる端末上の ChatGPT デスクトップを使用します。ChatGPT Web はローカルの Codex MCP 設定を読み取らないため、非公開のローカル設定だけでは利用できません。なお、ChatGPT 以外では Codex CLI や IDE extension など、同じネットワークから到達できる MCP クライアントも利用できます。

公開構成では、remote MCP を含むプラグインを ChatGPT Web に導入することで利用できます。ただし、MCP の URL を単にインターネットへ公開するだけでは完了しません。プラグインの構成、利用する ChatGPT プラン・ワークスペースでの提供状況、管理者による許可なども必要です。詳細は [OpenAI の MCP ドキュメント](https://learn.chatgpt.com/docs/extend/mcp) を確認してください。

インターネットへ公開する場合は、`.env` で Bearer 認証を必ず有効にします。設定名は `API_TOKEN` です。

```dotenv
AUTH_ENABLED=true
API_TOKEN=<十分に長いランダムなトークン>
MCP_ALLOWED_HOSTS=obsidian-api.example.com
```

`MCP_ALLOWED_HOSTS` は公開時だけの設定ではなく、MCP を利用するすべての構成で必須です。`/mcp` に届くリクエストの `Host` ヘッダーとして実際に使われる値を指定します。Caddy などのリバースプロキシ経由の非公開構成でも、LAN や Tailscale 用のホスト名を `Host` として転送する場合はその値を指定してください。複数の値を許可する場合はカンマ区切りで指定します。

`API_TOKEN` は前後の空白を除いて16文字以上が必須です。この設定値はページネーションカーソルの署名にも使われるため、公開・非公開や `AUTH_ENABLED` の値にかかわらず必要です。ただし、最低文字数を満たすだけの短い文字列や、人が考えた推測可能な文字列は使用しないでください。次のコマンドで、64文字のランダムな16進文字列を生成できます。

```bash
openssl rand -hex 32
```

生成結果を `.env` の `API_TOKEN` に設定し、ChatGPT デスクトップ、プラグイン、その他の MCP クライアント側にも同じ値を Bearer token として設定します。`.env` やトークンを Git に commit してはいけません。`MCP_ALLOWED_HOSTS` には、`/mcp` に届く `Host` ヘッダーの値を指定します。

インターネット公開は開発者の検証範囲外です。Vault の内容を扱うため、公開する場合は HTTPS、認証、アクセス制御、ログ監視、レート制限などを含めて別途セキュリティ設計を行ってください。現行リポジトリで実運用確認されているのは、公開しない構成です。

### REST API の位置づけ

REST API も実装されていますが、主インターフェースは MCP です。REST API はヘルスチェック、診断、テスト、MCP を利用しないクライアント向けの補助機能として残されています。

開発者自身は REST API を日常運用では使用していません。そのため、主要な MCP フローと比べると、実利用で見つかる使いにくさや設計上の不足が残っている可能性があります。通常は MCP を使用し、REST API を採用する場合は事前に必要な操作とエラー処理を検証してください。

## 運用全体の流れ

会話の作成から Vault 内の最終配置までは、次の役割分担で進みます。

```text
ChatGPT / ChatGPT Desktop app
  │ 会話を構造化して保存を依頼
  ▼
Obsidian Vault Gateway
  │ 安全性を検証して新規保存・明示的追記
  ▼
00_Inbox/ChatGPT
  │ 未整理ノートをレビュー・分類・統合・分割
  ▼
Codex / 人間
  ├─→ Notes
  ├─→ Journal
  ├─→ Projects
  └─→ Archive
```

ChatGPT は会話内容を保存用に構造化し、Gateway はそれを `00_Inbox/ChatGPT` に安全に投入します。Gateway の処理はここで終了します。その後、Codex または人間が Inbox を確認し、既存ノートとの関係や内容の性質を判断して、Vault 内の適切なディレクトリへ配置します。

検索・参照の場合、Gateway は Inbox を経由せず Vault 全体を読み取れます。ただし、検索で見つけたノートを移動・更新・削除することはありません。

## 基本の Vault 構成

複雑な分類体系を最初から作らず、次の構成を基本とします。

```text
Vault/
├─ 00_Inbox/
│  └─ ChatGPT/
├─ Notes/
├─ Journal/
├─ Projects/
└─ Archive/
```

### 各ディレクトリの意味

| ディレクトリ | 置くもの | 判断の軸 |
|---|---|---|
| `00_Inbox/ChatGPT` | Gateway が新規保存する未整理ノート | まだ整理していないか |
| `Notes` | 長期的に残す情報、知識、記録、継続対象のノート | テーマ・対象 |
| `Journal` | 日記、日次メモ、出来事の記録 | 日付・時系列 |
| `Projects` | 明確な目的と終了条件がある進行中の作業 | 現在進行中か |
| `Archive` | 終了済み、退避済み、日常の作業対象から外したもの | 現役でないか |

`Notes` は Knowledge、Records、Areas、Reference などに細分化しません。これらは意味として区別できても、通常の保存先として別トップレベルディレクトリを増やす必要はありません。迷ったらまず `Notes` に置きます。

`Journal` は日付が主役です。`Journal/2026/2026-08-09.md` のように年だけ分けてもよく、既存の Daily Note 運用がある場合はそれに合わせます。

`Projects` は「やること」が明確なものだけに使います。継続的に管理する対象や、その対象についての知識・履歴は `Notes` に置けます。終了した Project は、必要に応じて `Archive` へ移すか、価値のある内容を `Notes` に整理します。

## Gateway と整理担当の境界

### Gateway が担当すること

- Vault 全体のノート検索
- ノート本文の参照
- Vault のディレクトリ構造の参照
- Vault 全体の概要・件数の参照
- 保存前に指定された関連ノートの存在確認
- `00_Inbox/ChatGPT` への新規ノート作成
- `00_Inbox/ChatGPT` 直下にある既存ノートへの追記

### Gateway が担当しないこと

- ノートの移動、リネーム、削除
- `00_Inbox/ChatGPT` 以外への書き込み
- 既存ノートの上書き
- ノートの統合、分割、再構成
- Vault 全体の知識重複の判定
- Inbox の自動昇格や自動分類
- 関連ノート側へのバックリンクや説明の自動追記

この制限は安全性のための設計です。Vault 全体は読み取り専用で扱い、書き込みは Inbox の二つの操作に限定します。書き込み操作はクライアント側でも確認を求める対象です。

## 会話を保存する仕組み

### 通常の保存

通常は、次のように自然言語で依頼します。

> この会話を Obsidian に保存して

クライアントは会話を読み、構造化された export を `create_inbox_note` に渡します。export mode を指定しない場合は `summary` が使われます。

保存されたノートは、選択された mode に応じた固定の見出し順と frontmatter を持ちます。タイトルから安全なファイル名が作られ、同名ファイルがある場合は `Title-2.md`、`Title-3.md` のように別名で新規作成されます。新規保存で既存ノートを上書きすることはありません。

Gateway は内容の意味を再解釈しません。たとえば「要約して保存」と頼んだ場合、要約するのはクライアントであり、Gateway は渡された構造化入力を決められた形式へレンダリングするだけです。

### export mode は分類先ではない

export mode は、ノートを `Notes` や `Projects` のどこへ置くかを示すものではありません。Inbox に保存する際、会話の情報をどの形式で保持するかを示す入力形式です。

| mode | 保持する情報 | 向いている会話 |
|---|---|---|
| `summary` | 概要、要点 | 一般的な相談・調査。既定値 |
| `technical` | 背景、設計、実装メモ、検証 | 技術設計、実装、検証結果 |
| `history` | 経緯、転換点 | 判断に至る流れ、会話の変化 |
| `full` | トピックごとの内容 | 長い会話を広く残したい場合 |
| `procedure` | 前提条件、手順、検証、ロールバック | 再実行可能な手順・作業記録 |
| `issue` | 症状、環境、調査、原因、回避策 | 障害調査、トラブルシュート |
| `reference` | 用語、事実、例 | 参照用の説明、定義、比較 |

利用者が mode を明示する必要はありません。内容に応じて「手順として保存」「障害調査として保存」「経緯も含めて保存」などと依頼すれば、クライアントが適切な mode を選べます。明示的に指定したい場合だけ、mode 名を使います。

### `procedure` はコマンドや設定ファイルもそのまま残せる

`procedure` の各手順は、説明文とコードを会話の順序どおりに交互に持てます。たとえば次のような依頼です。

> Compose ファイルに環境変数を追加して再起動する手順を、実行したコマンドと設定内容も含めて手順として保存して

このとき保存されるノートの `## 手順` は次のようになります。

````markdown
## 手順

1. 設定ファイルを開く。

   ```bash
   vi compose.yaml
   ```

   次の設定を追加する。

   ```yaml
   environment:
     FOO: bar
   ```
2. 保存後に再起動する。

   ```bash
   docker compose up -d
   ```
````

コマンドや設定ファイルの改行・インデントはそのまま保持されます（ADR-0009）。手順に直接属さない完成版の設定ファイルやスクリプトは、`## コード` という別セクションに残せますが、これは手順の各ステップに書かれたコードを移す場所ではありません — 手順の順序を保つため、ステップに属するコードは常にそのステップ内に残ります。

### 保存されたノートの共通構造

構造化 export は、mode にかかわらず次の共通部分を持ちます。

```markdown
## 要約
## 決定事項
<!-- mode-specific sections -->
## 未解決の論点
## 次のアクション
## 関連ノート
## 出典
```

空の項目も省略せず、`なし` などのプレースホルダーで表現されます。これにより、保存された会話は後から読んだときに自己完結したソース記録になります。Inbox からすぐに整理されなかった場合でも、何を決め、何が未解決で、何を参照したかを確認できます。

### 既存ノートへの追記

追記は、次の条件をすべて満たす場合だけ使います。

- ユーザーが「追記」「追加」「続きとして書く」などと明示している
- 対象ノートが特定できている
- 対象ノートが `00_Inbox/ChatGPT` の直下に既に存在する

たとえば、次の依頼です。

> `00_Inbox/ChatGPT/API 設計メモ.md` に今回の検証結果を追記して

`append_inbox_note` は新規作成しません。サブディレクトリ、`Notes`、`Journal`、`Projects`、`Archive` にあるノートも対象にできません。

単に「この会話を保存して」と依頼された場合は新規保存です。既存ノートに似た内容があっても、明示的な追記指示なしに append を選びません。

## 重複検出の意味

`find_duplicate_candidates` は、保存前の未整理重複を減らすための補助機能です。Vault 全体の知識重複を検出する機能ではありません。

具体的には、次の範囲だけを調べます。

- `00_Inbox/ChatGPT` の直下にあるノート
- ノートの frontmatter にある `title`、`project`、`tags`
- 提案されたタイトル、project、keywords

ノート本文は読みません。`Notes`、`Journal`、`Projects`、`Archive` に移動済みのノートとの重複や、本文の意味的な類似性は判定しません。

結果の `recommendation` は、書き込みを強制的に許可・拒否するものではありません。

| recommendation | 意味 | クライアントの動作 |
|---|---|---|
| `create` | 信頼できる重複候補がない | 確認なしで新規保存してよい |
| `confirm` | 高信頼度の候補が一つ | ユーザーに新規・追記・キャンセルを確認する |
| `choose` | 複数または曖昧な候補がある | 候補を示し、ユーザーに選択してもらう |

`confirm` や `choose` になっても、Gateway が自動的に write を拒否するわけではありません。これはクライアントが守るべき利用フローです。ユーザーの選択なしに新規保存や追記を進めないことが重要です。重複検出そのものが失敗した場合も、ユーザーが厳密な重複確認を要求していなければ、通常の新規保存を続けられます。

## 関連ノートの扱い

関連ノートは、Gateway が Vault 全体から勝手に推測して書くものではありません。保存するクライアントがまず `search_notes` などで候補を探し、Vault 相対の `.md` パスを `related_notes` として渡します。

Gateway は保存直前に各パスを再検証し、実在して安全なものだけを次のような wikilink として `## 関連ノート` に書きます。

```markdown
## 関連ノート

- [[Notes/Software/API設計]]
- [[Projects/Documentation-Update]]
```

次のような候補は保存を止めずに除外されます。

- 既に存在しないパス
- 重複したパス
- 危険な文字を含むパス

実際にリンクされた数と除外された数は、作成結果の `related_notes_linked` と `related_notes_skipped` で確認します。クライアントがパスを推測してはいけません。検索結果から得たパスをそのまま渡してください。

関連ノートへのリンクを作るだけで、関連ノート自体は変更しません。関連ノート側へのバックリンク、統合、説明の追記は、必要に応じて後の整理フェーズで行います。

## ケース別の使い方

### 1. 普通の会話を保存する

```text
この会話を Obsidian に保存して。
```

通常は `summary` で `00_Inbox/ChatGPT` に新規保存されます。保存後の最終配置を決める必要はありません。

### 2. 技術調査を保存する

```text
この会話の技術的な背景、設計、実装メモ、検証結果を Obsidian に保存して。
```

`technical` が適しています。保存先は `Notes` ではなく、いったん必ず Inbox です。

### 3. 再実行する手順を保存する

```text
この会話を、前提条件・手順・検証・ロールバックを含む手順書として Obsidian に保存して。
```

`procedure` が適しています。後で Codex が `Notes` の手順ノートや、進行中なら `Projects` の作業資料へ整理できます。

### 4. 障害調査を保存する

```text
この障害調査を、症状・環境・調査内容・原因・回避策として Obsidian に保存して。
```

`issue` が適しています。原因が分かっていない場合も、未解決として保存できます。

### 5. 経緯や判断を保存する

```text
この会話で、どのような経緯で今の方針になったかを含めて Obsidian に保存して。
```

`history` が適しています。ADR や Project の判断材料として後で参照できます。

### 6. 用語や事実を参照用に保存する

```text
この会話を、用語・事実・例が分かる参照ノートとして Obsidian に保存して。
```

`reference` が適しています。長期的に残す場合は、後で `Notes` に整理します。

### 7. 既存の Inbox ノートへ追記する

```text
00_Inbox/ChatGPT/API 設計メモ.md に、今回の比較結果を追記して。
```

対象パスを特定でき、追記が明示されている場合だけ `append_inbox_note` を使います。対象が曖昧な場合は、先に検索して候補を示すか、ユーザーに確認します。

### 8. Vault 内のノートを探して読む

```text
Vault 全体から認証方式に関するノートを探して、関連するものを読んで要約して。
```

この操作は読み取りだけなので、保存確認は不要です。正確なパスが分からないときは検索してから読みます。

## Inbox から Codex で整理する流れ

Gateway で保存した後の整理は、次のように Codex へ依頼します。

1. `00_Inbox/ChatGPT` の未整理ノートを一覧・読解する
2. 各ノートが自己完結しているか、出典・決定事項・未解決事項を確認する
3. Vault 全体を検索し、既存ノートとの重複・関係・統合候補を調べる
4. 内容を次の基準で行き先候補へ分ける
   - 日付主体の出来事・感想 → `Journal`
   - 明確に進行中の作業 → `Projects`
   - 長期的に残す情報・知識・記録 → `Notes`
   - 終了済み・退避用 → `Archive`
5. 必要に応じて、ノートを移動・リネーム・統合・分割する
6. wikilink、frontmatter、出典を修正する
7. 元の Inbox ノートを残すか、移動するか、削除するかを判断する

保存直後の Inbox ノートは、将来の整理元となる会話アーカイブです。昇格されることを前提に内容を失わせてはいけません。整理しないままでも読めるように保存されるため、すぐに別ノートへ移せない場合は保留できます。

整理時に既存ノートと統合する場合は、元の会話がどこから来たか分かるよう、必要に応じて次のいずれかを残します。

- 元の Inbox ノートへの wikilink
- 元ノートのパスやタイトルを出典として記載
- 会話由来であることを frontmatter や本文に記載

初回保存の時点で、Gateway が自動的に元ノートへ「昇格先」を追記することはありません。元ノートと整理後ノートの相互リンクが必要なら、整理フェーズで明示的に行います。

### Codex に依頼する標準例

```text
00_Inbox/ChatGPT をレビューしてください。

- 各ノートを読み、内容を要約してください。
- Vault 全体を検索して、既存ノートとの重複・統合候補を確認してください。
- 日付主体の内容は Journal、明確な進行中作業は Projects、
  長期的に残す知識・記録は Notes、終了済み・退避用は Archive を候補にしてください。
- 移動・統合・分割・削除を実行する前に、対象と変更内容を一覧で提示してください。
- 実行する場合は、出典と wikilink を維持し、Inbox の元ノートを残すかどうかも明示してください。
```

より自動的に進めたい場合でも、最初は「候補を提示」までにして、実際の移動・統合・削除は確認後に行う運用が安全です。

## ディレクトリ設計の原則

- トップレベルディレクトリは、運用方法が変わる単位だけにする
- `Notes` の中で Knowledge、Records、Areas などを必須分類にしない
- `Notes` 内のテーマ別サブディレクトリは、検索しやすい範囲で浅く保つ
- 日付が主キーなら `Journal`、テーマや対象が主キーなら `Notes`
- 終了条件のある作業だけを `Projects` に置く
- 完了したものを無理に再分類せず、必要なら `Archive` に退避する
- フォルダで分類しきれない関係は、wikilink・backlink・タグ・frontmatter で表す
- Gateway のために新しいトップレベルディレクトリを増やさない

この構成は固定された完全な分類体系ではありません。Vault が大きくなってから、本当に運用方法が変わる問題が見つかった場合だけ、追加のディレクトリを検討します。

## 迷ったときの判断表

| 迷っている内容 | 基本の判断 |
|---|---|
| 普通の調査・情報・履歴 | `Notes` |
| 日記・日次メモ・日付の出来事 | `Journal` |
| 期限や終了条件のある作業 | `Projects` |
| 完了済み・退避済み | `Archive` |
| ChatGPT から保存した直後 | `00_Inbox/ChatGPT` |
| どこへ置くか決められない | まず `Notes`、または Inbox のまま保留 |

## 関連ドキュメントとの関係

この運用は、次の設計方針に基づきます。

- README: MCP を主インターフェースとし、書き込みを Inbox の create/append に限定する
- ADR-0005: `create_inbox_note` を構造化 chat export の単一入口とし、7 つの export mode を提供する
- ADR-0006: 関連ノートはクライアントが候補を選び、Gateway が保存時に再検証する
- ADR-0007: duplicate detection は Inbox 直下だけを対象とする読み取り専用・助言的な機能とする
- ADR-0009: `procedure.steps` はコードと説明文の順序を保持し、コードは `## コード` へ集約しない
- Issue #15: 会話アーカイブと長期的な知識を分け、昇格・整理は別のレビュー工程として扱う

このため、export mode を Vault の分類名として使ったり、duplicate detection を Vault 全体の知識監査として扱ったり、Gateway に移動・統合・削除を追加したりしません。